<h1>{{title}}</h1>
<p>Only authorized personnel see this page</p>


{{#if clients}}
<div>
    {{#each clients}}
    <div class="my-5 bg-white border-gray-400 rounded shadow">
        <div class="flex p-3">
            <div class="flex-1">
                <h5 class="block text-lg font-bold">{{first_name}} {{last_name}}</h5>
                <p class="block font-semibold">Company: {{company}}</p>

            </div>
            <div class="flex items-center justify-center">

                <a href="clients/{{_id}}" class="m-3 btn">Client Profile</a>
                <a href="" class="m-3 btn">Create Support Ticket</a>
            </div>
        </div>
    </div>
    {{/each}}
    </ul>
    {{else}}
    No clients
    {{/if}}